const { ERC20 } = require('./erc20')
const { Token } = require('./token')
const { RelayerRegistration } = require('./relayer_registration')

module.exports = {
  ERC20,
  Token,
  RelayerRegistration,
}
